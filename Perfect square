import java.util.*;

public class Main{
    static boolean isPerfectSquare(int n) {
        int r = (int)Math.sqrt(n);
        return r * r == n;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();

        for (int i = 1; i <= N; i++)
            if (isPerfectSquare(i)) System.out.print(i + " ");
    }
}
